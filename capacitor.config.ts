import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'chat.tutorial',
  appName: 'chat-tutorial',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
